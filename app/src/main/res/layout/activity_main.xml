<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/multicastIP"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:ems="10"
        android:maxLength="15"
        android:hint="Multicast IP Address" />

    <EditText
        android:id="@+id/multicastPort"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/multicastIP"
        android:ems="10"
        android:inputType="number"
        android:maxLength="6"
        android:hint="Multicast Port" />

    <TextView
        android:id="@+id/consoleView"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_below="@id/multicastPort"
        android:text="Console" />

    <EditText
        android:id="@+id/messageToSend"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/consoleView"
        android:maxLength="64"
        android:hint="Message to Send" />

    <Button
        android:id="@+id/clearConsoleButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/messageToSend"
        android:text="Clear Console" />

    <Button
        android:id="@+id/sendMessageButton"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/messageToSend"
        android:layout_toRightOf="@id/clearConsoleButton"
        android:text="Send Message" />

</RelativeLayout>
